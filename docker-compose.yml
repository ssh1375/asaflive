services:
  postgres:
    image: postgres:18
    container_name: asaflive-postgress
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgress
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql
